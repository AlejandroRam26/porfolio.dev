---
import NavbarItem from "./NavbarItem.astro";
import {avatar, name} from "../data/porfolio_data.json";
---

<header class="flex justify-between items-center py-5 px-3 fixed left-0 right-0 top-0 w-full xl:w-[1120px] z-50 mx-auto">
    <!-- <img
        class="rounded-md size-12"
        src={avatar}
        alt={`${name} - photo`}
    /> -->
    <div class:list={[
        "rounded-md size-14 bg-blue-900/80 border-2 border-blue-400 bg-center bg-cover bg-no-repeat",
        "hover:scale-125 transition"
    ]}
    style={`background-image: url(${avatar});`}></div>
    <nav class="flex flex-row gap-x-3 opacity-50 bg-indigo-950 rounded-md hover:bg-indigo-700 hover:opacity-80 transition">
        <NavbarItem href="#top">
            Inicio
        </NavbarItem>
        <NavbarItem href="#experiencia">
            Experiencia
        </NavbarItem>
        <NavbarItem href="#proyectos">
            Proyectos
        </NavbarItem>
        <NavbarItem href="#sobre-mi">
            Sobre mí
        </NavbarItem>
        <NavbarItem href="#contacto">
            Contacto
        </NavbarItem>
        <NavbarItem href="#habilidades">
            Habilidades
        </NavbarItem>
    </nav>
</header>
